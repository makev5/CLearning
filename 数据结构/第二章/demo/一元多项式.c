#include <stdio.h>
/*
    一元多项式的运算：pn(x) = p0+p1x+p2x^2+...+pnx^n 
        例子：A = 7 + 3X + 9X^8 + 5X^17
			  B = 8X + 22X^7 - 9X^8	 
        一、稀疏多项式(空间复杂度高) 
	        线性表A = ((7,0), (3,1), (9,8), (5,17)) 
	        线性表B = ((8,1), (22,7), (-9,8))
			(1)创建一个新数组c
			(2)分别从头遍历比较a和b的每一项
							指数相同，对应系数相加，若其和不为零，则在c中增加一个新项
							指数不同，则将指数较小的项复制到c中
			(3)一个多项式已遍历完毕时，将另一个剩余项依次复制到c中即可 (数组c多大合适呢？) 
        二、链式存储结构 
		    
*/
int main() 
{
	return 0;
} 
